<project name="GenePatternRegressionTest" default="junit">
	<property name="src" location="src"/>
	<property name="lib" location="lib2"/>
	<property name="build" location="build"/>
	<property name="dist" location="dist"/>
	<path id="project.class.path">
		<fileset dir="lib2" includes="gp-full.jar,gp-modules.jar"/>
	</path>
	
	<target name="clean">
		<delete dir="${build}"/>
		<delete dir="${dist}"/>
	</target>

	<target name="compile" depends="clean" 
	        description="compiles the source code">
		<mkdir dir="${build}"/>
		<copy todir="${build}">
		  <fileset dir="${src}">
		    <filename name="log4j.properties"/>
		  </fileset>
		</copy>
		<javac srcdir="${src}" destdir="${build}" debug="on">
		  <classpath> 
		    <fileset dir="lib2">
		      <include name="**/*.jar"/>
		    </fileset>
		  </classpath>
		</javac>
	</target>

	<target name="junit" depends="compile"
	        description="runs junit tests">
		<junit fork="yes">
		  <test name="org.genepattern.integration.TestAll" outfile="test_all"/>
		  <classpath>
		    <fileset dir="lib2" includes="**/*.jar"/>
		    <dirset dir="build"/>
		  </classpath>
		  <formatter type="xml"/>
		  <jvmarg value="-DgenePatternUrl=http://gpdev02.broad.mit.edu:8080"/>
		  <jvmarg value="-Dusername=jntest"/>
		  <jvmarg value="-Dpassword=jntest"/>
		</junit>
	</target>
</project>
