To build the package for Unix and Mac, run the target create-gzip in the ant build script. To build the package for Windows, install the package on Mac or Unix and then zip the installed GenePattern package directory.